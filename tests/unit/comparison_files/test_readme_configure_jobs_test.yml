stages:
- print_date
print-date:
  image: docker/image:example
  stage: print_date
  script:
  - ./before-script.sh
  - date
  - ./after-script.sh
  variables:
    USER: Max Power
    URL: https://example.com
  rules:
  - if: $MY_VARIABLE_IS_PRESENT
    when: on_success
    allow_failure: false
  artifacts:
    paths:
    - binaries/
    - .config
  tags:
  - test
  - europe
